/**
 * @file main.hpp
 * @author vss2sn
 * @brief Just includes includes. Needs to be refactored.
 */

#ifndef BUILD_INDIVIDUAL  // Only to make sure main is is included in
                          // documentation
#ifndef TEST  // Only to make sure main is is included in documentation
/**
 * @brief Script main function. Generates start and end nodes as well as grid,
 * then creates the algorithm objects and calls the main algorithm functions.
 * @return 0
 */
int main();

#endif  // BUILD_INDIVIDUAL
#endif  // TEST
